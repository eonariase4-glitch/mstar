{
  "name": "dealsight",
  "version": "0.1.0",
  "private": true,
  "description": "DealSight UK property intelligence starter project.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace client & npm run dev --workspace server",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server"
  }
}
